PORT=8787
CLIENT_ORIGIN=http://localhost:5173
AI_PROVIDER=mock
AI_API_KEY=
# mock (safe fallback), coingecko (crypto public API), brapi (Brazilian quotes), or real (both)
MARKET_DATA_PROVIDER=mock
COINGECKO_BASE_URL=https://api.coingecko.com/api/v3
BRAPI_BASE_URL=https://brapi.dev/api
# Required only for providers/limits that require it; keep server-side.
BRAPI_TOKEN=
DATABASE_URL=
TRADE_EXECUTION_ENABLED=false
