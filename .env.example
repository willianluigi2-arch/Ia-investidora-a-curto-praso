PORT=8787
CLIENT_ORIGIN=http://localhost:5173
AI_PROVIDER=mock
AI_API_KEY=
MARKET_DATA_PROVIDER=mock
# coingecko uses a public endpoint; no key is required for the baseline adapter.
COINGECKO_BASE_URL=https://api.coingecko.com/api/v3
# Future licensed Brazilian provider (backend only):
BRAPI_TOKEN=
# Reserved for future persistence; never expose it to the browser.
DATABASE_URL=
# Real execution is intentionally not configurable yet.
TRADE_EXECUTION_ENABLED=false
